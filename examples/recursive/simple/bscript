from bento.commands.hooks import recurse, pre_configure, override

@recurse(["foo/bscript", "bar/bscript"])
@pre_configure
def pconfigure(ctx):
    for ext_name, ext in ctx.pkg.extensions.items():
        print ext
