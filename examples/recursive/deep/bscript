from bento.commands.hooks \
    import \
        post_configure, recurse

@recurse(["foo/bar2/bscript", "foo/bar1/bscript"])
@post_configure
def configure(ctx):
    yctx = ctx.yaku_configure_ctx
    yctx.use_tools(["ctasks"])
